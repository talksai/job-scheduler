# Load tests (M5)

k6/Gatling profiles to push the scheduler to the target jobs/day and capture SLO numbers
(scheduling-accuracy p99, throughput, DLQ rate).

Nothing here yet — see `projects/02-job-scheduler/BUILD-PLAN.md` M5.
