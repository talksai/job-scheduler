global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: job-scheduler
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["app:8080"]
